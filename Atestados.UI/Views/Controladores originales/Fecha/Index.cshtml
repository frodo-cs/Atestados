@model IEnumerable<Atestados.Objetos.Dtos.FechaDTO>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FechaInicio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FechaFinal)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Atestado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FechaInicio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FechaFinal)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Atestado)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FechaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.FechaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FechaID })
        </td>
    </tr>
}

</table>
